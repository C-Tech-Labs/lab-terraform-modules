# Examples

This directory contains sample Terraform configuration files demonstrating how to consume the modules provided in this repository. Use these examples as a starting point for your own projects or for testing modules in isolation.

## Available examples

- `vpc-basic` – provisions a dual-AZ VPC with public and private subnets, a NAT gateway, and flow logs enabled by default.

To try an example, change into its directory and run `terraform init` followed by `terraform validate` and `terraform apply` against your AWS account.
