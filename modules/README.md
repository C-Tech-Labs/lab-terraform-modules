# Modules

This directory contains reusable Terraform modules for lab environments. Each module is self-contained with variables, outputs, and accompanying examples in the `examples/` directory.

## Available modules

- `vpc` – creates a VPC with public and private subnets, NAT gateway support, flow logs, and consistent tagging.

See each module's README for input/output details and usage guidance.
